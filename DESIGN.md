# claude-pet — 設計書

Claude Code のマスコット（オレンジのピクセルキャラ）を、ターミナル風の Web アプリの中で飼う「インタラクティブ・ペット」。
クリックで反応し、マウスを追いかけ、餌を与えると XP が貯まって進化する。

## 1. 技術選定

| 項目 | 選択 | 理由 |
| --- | --- | --- |
| 言語 | TypeScript (strict) | 提案どおり。状態機械・ピクセルマップを型で守れる |
| ビルド | Vite | 設定ほぼゼロ、Vercel が自動検出（`npm run build` → `dist/`） |
| 描画 | Canvas 2D（ライブラリ無し） | ピクセルアートは `fillRect` の集合で十分。依存ゼロで軽い（gzip 11 KB） |
| UI | 素の DOM + CSS | パネルはテキスト中心なのでフレームワーク不要 |
| 永続化 | `localStorage` | サーバー不要で Vercel の静的ホスティングにそのまま乗る |
| テスト | Vitest | 純粋ロジック（餌の効果・減衰・進化しきい値・スプライト整合性）のみ対象 |

React 等を入れない判断: 60fps のゲームループと DOM の再レンダリングを混ぜると面倒になる。
ロジック（`pet.ts`）と描画（`renderer.ts`）と DOM（`ui.ts`）を分けておけば後からフレームワークに載せ替えるのも容易。

## 2. 画面構成（Claude Code の雰囲気）

```
┌ ● ● ●          claude-pet — ~/pets/claude              v0.1.0 ┐
│                                     │ ▎status                   │
│        (canvas: ペットの遊び場)      │  claude · Haiku · lv 1    │
│   ・ドットグリッド背景               │  hunger [████████░░░░] 70% │
│   ・ペット / 餌 / 吹き出し / 粒子    │ ▎feed  [cursor][token]...  │
│   ・マウス位置に点滅するブロックカーソル│ ▎log                      │
│                                     │  ⏺ Eat(token)              │
│                                     │    ⎿ +22 hunger · +10 xp   │
│                                     │ > /help for commands       │
└ ✻ claude · idle        snacks 0 · uptime 00:01:23 · autosave ✓ ┘
```

- **配色**: 背景 `#141413`、アクセントは Claude オレンジ `#D97757`、クリーム `#F4F3EE`。
- **ログ**: Claude Code の出力を模した接頭辞（`⏺` アクション / `⎿` 結果 / `✻` システム / `>` ユーザー入力）。
- **プロンプト**: 最下部に `>` の入力欄。スラッシュコマンドで操作できる。
- **ステータスライン**: 名前・気分・スナック数・稼働時間・自動保存表示。
- レスポンシブ: 860px 以下で縦積み（キャンバスが上、パネルが下でスクロール）。

## 3. インタラクション

| 操作 | 反応 |
| --- | --- |
| ペットをクリック / タップ / Space | **腕を上げるポーズ**でぷにっと潰れて跳ねる、ハート粒子、吹き出し（"hey!" など）、joy +3 |
| マウス移動 | 目線がカーソルを追う。カーソルが 96px 以上離れていると**這うポーズ**（尻尾つき）で追いかける（2.5 秒動きが無ければ諦める） |
| 空きスペースをクリック | そこまで歩く |
| 餌を選んで（1–4 キー or トレイ）クリック | その場所に餌を置く。最寄りの餌へ歩いて行き 1.5 秒かけて食べる |
| `/feed <kind>` コマンド | ランダムな場所に餌を置く |
| 放置 | 4–11 秒ごとにうろうろ。60 秒以上放置＋energy < 60 で寝る。energy < 6 で強制睡眠 |
| 睡眠中にポーク | 起きる |
| 12–26 秒ごと | 状態に応じた独り言（"✻ Thinking…", "need tokens", "*yawn*"…） |

### 餌（Claude Code ネタ）

| 餌 | hunger | energy | joy | xp | 追加効果 |
| --- | --- | --- | --- | --- | --- |
| token ▮ | +22 | 0 | +4 | +10 | 基本食 |
| coffee ☕ | +4 | +35 | +6 | +8 | 12 秒間 移動速度 1.7 倍 |
| bug 🐛 | +8 | −4 | +22 | +15 | バグを直す＝大満足 |
| commit ◆ | +10 | +5 | +12 | +30 | 大量 XP |

hunger > 92 のときに食べさせると **overfed**（joy −6、xp 半分、"rate limited" と嘆く）。

## 4. ステータスと減衰

`Stats = { hunger, energy, happiness, xp }`（0–100、xp は無制限）。

- 起きている間（毎秒）: hunger −0.08、energy −0.06、happiness −0.04（hunger < 25 または energy < 15 なら −0.12）。
- 睡眠中: energy +1.2/秒、hunger −0.03/秒。energy ≥ 96 で自然に起きる。
- 離席中（`savedAt` からの経過時間、上限 2 時間）: ゆるやかに減衰、energy は回復。**死なない**設計。
- 気分の導出: `sad`（hunger < 20 or joy < 25 or energy < 12）/ `happy`（joy > 75 and hunger > 40）/ `idle`。
  食後・ポーク後などは一時的な気分（happy/sad/excited）がタイマー付きで上書きする。

## 5. 進化と着せ替え

XP しきい値で 4 段階。本体のドット絵は共通で、ドットの大きさが少しずつ増え、**ステージごとに衣装が解放**される。
解放と同時に自動で着せ替わり、`/wear <name>` で解放済みの衣装を好きに切り替えられる（`none` で脱ぐ）。

| stage | 名前 | 必要 xp | 解放される衣装 | ドット | 速度 |
| --- | --- | --- | --- | --- | --- |
| 1 | Haiku | 0 | （なし） | 6 px | 75 px/s |
| 2 | Sonnet | 60 | `hardhat` ヘルメット＋レンチ（腕を上げるとレンチも上がる） | 6 | 88 |
| 3 | Opus | 180 | `wizard` 魔法使いの帽子 | 7 | 98 |
| 4 | Mythos | 420 | `party` 王冠＋降り続ける紙吹雪 | 8 | 112 |

進化演出: 1.8 秒間 白点滅＋震え → ジャンプ → 火花粒子 40 個、ログに `✻ Compacting context… / Evolved: Haiku → Sonnet` と `Unlocked outfit: …`。

## 6. スプライト表現

1 行 1 文字＝1 ピクセルの文字列配列（`src/sprites.ts`）。本体は参考画像に合わせて 11×7 の角丸ボディ、1 ドットの目、左右 1 ドットの腕、細い脚 4 本。

```
'...#########....'
'..###########...'
'..##@#####@##...'   # = 本体   @ = 目   . = 透明
'.#############..'   ← 腕
'.#############..'
'..###########...'
'..###########...'
'....#.#.#.#.....'   ← 脚
'....#.#.#.#.....'
```

- **ポーズ**は 3 種（`idle` / `armup` 腕上げ / `crawl` 這う＋灰色の尻尾）。全部 16×9 の同じキャンバスに描くので、切り替えても本体がずれない。
  `armup` はポーク・進化・興奮時、`crawl` はカーソルを追いかけている間。左向きのときは水平反転。
- 目は `@` 位置に**レンダラが後から描く**。1 ドットの目を半ドットずらして「視線がカーソルを追う」、細い線で「瞬き / 笑い目 / 悲しい目」を表現。
- 最下段（idle は 2 段）は脚。歩行中は奇数/偶数の脚を交互に非表示にしてちょこちょこ歩きに見せる。
- 食事中は目の下に口を描いて開閉。
- **衣装**は本体の後に重ねるオーバーレイ（`OUTFITS`）。idle 座標系で `x, y` を指定し（負の y = 頭の上）、ポーズごとの頭の位置ずれを `POSE_HEAD_OFFSET` で補正。ポーズ限定のレイヤも持てる（レンチは腕上げ時だけ位置が変わる）。
- 検証: `validateMap()` で全行の幅と使用文字をチェック（dev 時に console、テストでも）。

## 7. モジュール構成

```
src/
  main.ts        ブート・入力・ゲームループ・イベント→UI の橋渡し
  pet.ts         Pet クラス: ステータス、行動決定（餌 > クリック先 > カーソル > 徘徊/睡眠）、アニメ状態、イベント発行
  renderer.ts    Canvas 描画（背景グリッド、スプライト、目、影、吹き出し、カーソル）
  particles.ts   ハート / 火花 / z / パンくず
  ui.ts          右パネル DOM（ステータス ASCII バー、餌トレイ、ログ、プロンプト）
  commands.ts    スラッシュコマンド
  logic.ts       純粋関数: 餌効果、減衰、気分導出
  evolution.ts   ステージ定義・xp→stage
  sprites.ts     ピクセルマップとパレット
  storage.ts     localStorage 読み書き
  types.ts       共有型
```

### 到達判定（Mythos が端の餌を取れなかったバグの対策）

- ペットが立てる領域は `clampFeet()` に一元化（本体幅の半分 + 帽子の高さ分だけ端から離す）。餌・クリック先・カーソル・うろうろ目標は**すべてこの領域にクランプ**してから歩くので、壁に向かって歩き続けることが無い。
- 餌を「食べられる」判定は足元との距離ではなく `canReach()`（本体＋帽子の当たり判定を 10px 広げた箱）。端に置かれた餌でも、立てるいちばん近い場所まで行けば箱に入るので食べられる。

`Pet` は DOM を知らない。`events` キューに `bubble / ate / evolved / poked …` を積み、`main.ts` が毎フレーム drain してログ・粒子・吹き出しに変換する。これでロジックのテストとレンダラの差し替えが独立する。

### フレームごとの流れ

1. `pet.update(dt, world)` — 減衰 → 瞬き/物理 → (進化中 | 食事中 | 睡眠中 なら早期 return) → 目的地決定 → 移動 → 独り言
2. `drainEvents()` — イベントを UI へ
3. `particles.update()`
4. 描画: 背景 → 餌 → ペット → 睡眠時の暗転 → 粒子 → 吹き出し → カーソル
5. ステータス DOM は差分があるときだけ書き換え。4 秒ごとに自動保存。

## 8. コマンド

`/help /feed <kind> /pet /status /name <name> /wear <outfit> /sleep /wake /stages /save /reset confirm /clear`
スラッシュ無しの入力にはペットが "I am a pet, not a chatbot ✻" などと返す。

## 9. 今後の拡張案

- ドラッグ＆ドロップで餌を落とす（現状はツール選択→クリック）
- 衣装の追加（帽子以外のアクセサリ、季節もの）と `/wear` の UI ボタン化
- ミニゲーム（`/test` で走らせるとランダムに pass/fail、fail は bug 餌をドロップ）
- 複数ペット / 名前ごとのセーブスロット
- 音（ポーク時のビープなど）
- Claude API 連携で吹き出しの台詞を生成
